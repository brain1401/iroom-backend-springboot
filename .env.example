# 데이터베이스 설정
DB_URL=jdbc:mysql://localhost:3306/your_database_name
DB_USERNAME=your_db_username
DB_PASSWORD=your_db_password

# AI 서버 설정
AI_SERVER_URL=http://localhost:8000

# AWS S3 설정 (인쇄 PDF 파일 저장용)
AWS_S3_REGION=ap-northeast-2
AWS_S3_BUCKET_NAME=your-s3-bucket-name
AWS_S3_PRESIGNED_URL_DURATION=60
AWS_S3_MULTIPART_MIN_PART_SIZE=8

# AWS 자격 증명 (AWS SDK에서 자동으로 인식)
# 다음 중 하나의 방법으로 AWS 자격 증명을 설정하세요:
# 방법 1: 환경 변수
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# 방법 2: AWS CLI 프로파일 (추천)
# AWS_PROFILE=your-profile-name

# 방법 3: IAM 역할 (EC2/ECS에서 실행 시)
# IAM 역할을 인스턴스에 연결하면 자격 증명이 자동으로 제공됨